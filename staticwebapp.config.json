{
  "routes": [
    {
      "route": "/api/get-embed-config",
      "methods": ["GET", "POST"],
      "rewrite": "/api/get-embed-config/index.js"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  }
}
